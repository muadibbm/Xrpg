//TODO : merge the funitonality of const script and content manager script
// Used to download all needed resources from the webserver
function ContentManager() {
	//Method returns when all elements have been downloaded
	var WINDOW_WIDTH = 0;
	var WINDOW_HEIGHT = 0;
}